---
# this playbook must be used only once to create the ansible user.

- name: Bootstrap ansible and creating ansible user
  remote_user: "{{ bootstrap_remote_user }}"
  become: yes
  hosts: all
  gather_facts: no
  roles:
    - bootstrap
...
